# P2 Shadow Architect checkpoint

Outcome: `CONTINUE_WITH_WARNINGS_L1`

## Material deltas closed

1. **Parent identity semantics — L2.** P0 semantic hashes, P1 formatting-sensitive hashes, raw bytes, and Git object IDs were previously conflated. Authoritative v3 receipts now expose semantic SHA-256 and exact Git blob SHA-1 separately; P2 packet v3 binds both plus provider CI.
2. **Schema monolith — L2.** The initial bundle duplicated every `$defs` block into every root. Definitions are now split into enum/base/inference/tools/orchestration/evidence resources; root wrappers use external Draft 2020-12 references.
3. **Generated language monolith — L2.** Kotlin and Swift are generated by domain family, reducing adapter coupling and future merge conflicts.
4. **Kotlin JSON escaping — L2.** The first generated canonical encoder emitted control characters instead of JSON escapes. Explicit character-code escaping plus a Kotlin/Swift byte-identical golden closes it.
5. **Swift reserved enum names — L2.** Values such as `STATIC`, `PRIVATE`, and `INTERNAL` generated invalid identifiers. Keyword-safe deterministic names now compile under Swift 6.2.1.
6. **Negative-control leakage — L2.** A canary initially embedded a forbidden private URL and resolver assignment in source. The public boundary gate rejected it; canaries are now assembled only at runtime.
7. **Generated drift and stale files — L1.** Manifest replay detects byte drift, missing outputs, and obsolete monolithic binding files.

## Falsifiers exercised

- required-field removal, enum reorder, and const version change;
- provider SDK type injection;
- unknown property and unversioned envelope;
- wrong parent Git blob and immutable ref;
- private Workspace URL, resolver value, and secret-like token;
- generated byte drift and stale generated file;
- evidence-lane laundering;
- unresolved external schema reference;
- Kotlin/Swift canonical wire mismatch.

## Residual warnings

- Kotlin compiled as a JVM package, not an Android Gradle module.
- Swift compiled on Linux, not against Xcode/iOS SDKs.
- GitHub Actions does not install Kotlin or Swift under the P2 writer lease; those results remain local-toolchain receipts.
- P1 signed-in retrieval, Drive ACL, private truth, and Human disclosure remain unexercised.
- Provider adapters, model artifacts, devices, model quality, privacy, energy, and thermal lanes remain downstream.
